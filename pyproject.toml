[project]
name = "research-synthesis-agent"
version = "0.1.0"
description = "Multi-Source Research Synthesizer - Agentic RAG"
readme = "README.md"
requires-python = ">=3.12"

# All Python dependencies are in backend/pyproject.toml
# This root file is for workspace metadata only
